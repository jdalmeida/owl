{
  "name": "owl",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '*' dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
